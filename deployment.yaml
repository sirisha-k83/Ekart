apiVersion: apps/v1
kind: Deployment
metadata:
  name: ekart
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: ekartapp
        image: sirishak83/ekart-app:latest
      imagePullSecrets:
      - name: dockerhub-secret
